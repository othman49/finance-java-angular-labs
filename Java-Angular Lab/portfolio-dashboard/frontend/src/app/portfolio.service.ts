import { Injectable } from '@angular/core';import { HttpClient } from '@angular/common/http';import { Observable } from 'rxjs';export interface Holding {  id: number;  symbol: string;  quantity: number;  avgCost: number;}@Injectable({ providedIn: 'root' })export class PortfolioService {  private baseUrl = 'http://localhost:8080/api/portfolio';  constructor(private http: HttpClient) {}  getHoldings(): Observable<Holding[]> {    return this.http.get<Holding[]>(this.baseUrl);  }  addHolding(symbol: string, quantity: number, avgCost: number): Observable<Holding> {    return this.http.post<Holding>(this.baseUrl, { symbol, quantity, avgCost });  }}